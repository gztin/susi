services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5679:5679"
    volumes:
      - ./backend/app:/app/app
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5678:5678"
    environment:
      - VITE_PROXY_API_TARGET=http://backend:5679
      - VITE_PROXY_WS_TARGET=ws://backend:5679
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    restart: unless-stopped
